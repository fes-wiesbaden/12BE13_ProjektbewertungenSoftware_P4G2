export interface LearningField {
  id: string;
  name: string;
  description: string;
  weightingHours: number;
}

export interface AddLearningfield {
  name: string;
  description: string;
  weightingHours: number;
}

export interface LearningField {
  id: string;
  name: string;
  description: string;
  weightingHours: number;
}
